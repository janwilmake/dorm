{
  "name": "dormroom",
  "version": "1.0.0-next.1",
  "files": [
    "DORM.ts"
  ],
  "main": "DORM.ts",
  "types": "types.d.ts",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250415.0"
  },
  "scripts": {
    "types": "tsc DORM.ts --declaration --emitDeclarationOnly --outFile types.d.ts --skipLibCheck --lib esnext --target es2015"
  }
}
